<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="MemberBoardVO">

	<!-- 회원가입 -->
	<insert id="MemberInsert" parameterType="MemberBoardVO">
		INSERT INTO
		member_metami
		(id, password, name)
		VALUES(#{id}, #{password}, #{name})
	</insert>
	
	<select id="loginCheck" resultType="MemberBoardVO">
		SELECT id, name FROM member_metami
		where id = #{id} and password = #{password}
	</select>
	<select id="viewMember" resultType="MemberBoardVO">
		SELECT id,name FROM member_metami
		where id = #{id} and password = #{password}
	</select>
	<!-- 회원탈퇴 -->
	<delete id="MemberDelete" parameterType="MemberBoardVO">
		DELETE FROM
		member_metami
		WHERE id=#{id}
	</delete>
</mapper>
